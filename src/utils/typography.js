import Typography from "typography";

const typography = new Typography({
    baseFontSize: "18px",
    baseLineHeight: 1.45,
    bodyFontFamily: ["TeleGrotesk Next", "serif"],
    headerFontFamily: [
        "TeleGrotesk Next",
        "Arial",
      ],
});

export default typography;